<!DOCTYPE html>
<html>
  <body>
    <p>Test with id:  <%= @test.id %></p>
    <p>Titile: <b><%= @test.title %></b></p>
    <p>Level: <%= @test.level %></p>
    <p>Category: <%= @test.category.title%></p>
    <p>Author: <%= @test.author.username %></p>
    <p>Number of questions: <%= questions_amount(@test)%></p>
    <p><b>Questions: </b></p>

    <%= render 'shared/questions_list', resource: @questions unless @questions.empty? %> 

    <%= link_to 'Back', action: 'index' %>
  </body>
</html>
