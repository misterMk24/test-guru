<tr>
  <td><%= test.title %></td>
  <td><%= test.level %></td>
  <td><%= test.category.title %>
  <td><%= test.author.username %></td>
  <td><%= questions_amount(test) %></td>
  <td><%= link_to 'Show', admin_test_path(test) %></td>
  <td><%= link_to 'Edit', edit_admin_test_path(test) %></td>
  <td><b><%= link_to 'Destroy', admin_test_path(test),
				method: :delete,
				data: { confirm: 'Are you sure?' } %></b></td>
</tr>
